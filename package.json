{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "ISC",
  "scripts": {
    "start": "json-server --watch db.json --port 3001"
  },
  "devDependencies": {
    "json-server": "^0.17.4"
  },
  "dependencies": {
    "react-redux": "^9.2.0"
  }
}
